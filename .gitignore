# --- CHẶN RÁC TOÀN DỰ ÁN (GLOBAL) ---

# 1. Thư viện nặng (Chặn tất cả node_modules ở mọi ngóc ngách)
node_modules/
*/node_modules/

# 2. File bảo mật (Tuyệt đối không đẩy lên)
.env
.env.local
.env.development
.env.production

# 3. File rác hệ điều hành & Editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# 4. File log lỗi
npm-debug.log*
yarn-debug.log*